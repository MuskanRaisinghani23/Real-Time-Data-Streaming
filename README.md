# Real-Time-Data-Streaming
The project demonstrates an end-to-end data pipeline using Apache Kafka to fetch and stream data from a website. 
The project is containerized with Docker for streamlined deployment and dependency management.
